# Copy to .env for local development (cargo run)

DATABASE_URL=postgres://wholesale_user:wholesale_password@localhost:5432/wholesale_db
JWT_SECRET=change-me-in-production
JWT_EXPIRY_HOURS=24
RUST_LOG=debug
